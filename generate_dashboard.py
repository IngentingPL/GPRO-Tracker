import json\n\n# Existing code...\ndef generate_html():\n    # Load prediction.json data\n    with open('prediction.json') as json_file:\n        prediction_data = json.load(json_file)\n        PREDICTION_DATA = prediction_data\n\n    # Existing HTML generation code...\n\n    # Embed PREDICTION_DATA as JavaScript variable in HTML\n    html += f'<script>const PREDICTION_DATA = {json.dumps(PREDICTION_DATA)};</script>'\n\n    return html\n\n# Existing code...\ndef renderNextRace():\n    # Utilize prediction data if available\n    if PREDICTION_DATA:\n        confidence_level = PREDICTION_DATA.get('confidence_level', 'N/A')\n        setup_adjustments = PREDICTION_DATA.get('setup_adjustments', 'N/A')\n        fuel_strategy = PREDICTION_DATA.get('fuel_strategy', 'N/A')\n        tire_recommendations = PREDICTION_DATA.get('tire_recommendations', 'N/A')\n        driver_margin = PREDICTION_DATA.get('driver_margin', 'N/A')\n        # Display these variables in your rendering logic accordingly\n    # Existing rendering logic...\n